<h1>New place</h1>

<% form_for(setup_place(@place)) do |f| %>
  <%= f.error_messages %>
  
  <%= render :partial => 'form', :locals => { :f => f } %>
  
  <p>
    <%= f.submit 'Save' %>
  </p>

<% end %>

<%= link_to 'Back', places_path %>